<Window x:Class="HeroArena.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:HeroArena.Views" Title="HeroArena" Height="720" Width="1100" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">

    <Border CornerRadius="12">
        <Border.Background>
            <StaticResource ResourceKey="BackgroundGradient"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
                <Border CornerRadius="12,12,0,0" Background="{StaticResource BackgroundDarkBrush}" Opacity="0.7"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="⚔" Foreground="{StaticResource AccentBrush}" FontSize="18" Margin="0,0,8,0"/>
                    <TextBlock Text="HERO ARENA" Style="{StaticResource TitleStyle}" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Button Content="—" Width="32" Height="28" Margin="0,0,4,0" Style="{StaticResource SecondaryButtonStyle}" Click="MinimizeButton_Click"/>
                    <Button Content="✕" Width="32" Height="28" Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <TabControl Grid.Row="1" Style="{StaticResource ModernTabControlStyle}" Margin="8">
                <TabItem Header="⚔ HÉROS" Style="{StaticResource ModernTabItemStyle}">
                    <local:HeroView/>
                </TabItem>
                <TabItem Header="✨ SORTS" Style="{StaticResource ModernTabItemStyle}">
                    <local:SpellView/>
                </TabItem>
                <TabItem Header="🗡 COMBAT" Style="{StaticResource ModernTabItemStyle}">
                    <local:CombatView/>
                </TabItem>
            </TabControl>

            <Border Grid.Row="2" Background="{StaticResource BackgroundDarkBrush}" Opacity="0.8">
                <Border CornerRadius="0,0,12,12" Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,16,0">
                        <TextBlock Text="Joueur : " Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="PlayerNameText" Foreground="{StaticResource GoldBrush}" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>
