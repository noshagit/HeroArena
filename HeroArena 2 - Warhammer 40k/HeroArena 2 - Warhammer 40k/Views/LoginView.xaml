<Window x:Class="HeroArena.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:HeroArena.Resources" Title="HeroArena" Height="580" Width="420" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource BackgroundDarkBrush}" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" Width="28" Height="28" Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40,0,40,40">

                    <TextBlock Text="⚔" FontSize="48" HorizontalAlignment="Center" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                    <TextBlock Text="HERO ARENA" Style="{StaticResource TitleStyle}" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,4"/>

                    <TextBlock Text="Entrez dans l'arène" Style="{StaticResource LabelStyle}" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,0,0,16"/>

                    <StackPanel Visibility="{Binding IsRegisterMode, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <TextBlock Text="NOM DU JOUEUR" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBoxStyle}" Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="NOM D'UTILISATEUR" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBoxStyle}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="MOT DE PASSE" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordBoxStyle}" PasswordChanged="PasswordBox_PasswordChanged"/>
                    </StackPanel>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,8,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button Content="{Binding LoginButtonText}" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,16,0,8"/>

                    <Button Content="{Binding ToggleText}" Command="{Binding ToggleModeCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,16"/>

                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,0,0,12"/>

                    <Button Content="Paramètres de DB" Command="{Binding OpenSettingsCommand}" Style="{StaticResource SecondaryButtonStyle}" FontSize="11"/>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
